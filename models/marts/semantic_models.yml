version: 2
semantic_models:
  - name: sales_metrics
    model: ref('fct_sales')
    measures:
      - name: total_revenue
        agg: sum
        expr: net_item_sales_amount
    dimensions:
      - name: order_date
        type: date